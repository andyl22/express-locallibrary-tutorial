extends layout 

block content 
  h1=title 
  
  p Are you sure you want to delete this book instance?

  form(method='POST', action='')
    div.form-group
      input#authorid.form-control(type='hidden',name='bookinstanceid', required='true', value=bookinstance_id )
      button.btn.btn-primary(type='submit') Delete